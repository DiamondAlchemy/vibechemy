appId: com.vibechemy.app
productName: Vibechemy
directories:
  buildResources: build
  # Keep packaging output outside the source tree.
  output: /tmp/vibechemy-dist
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
extraResources:
  - from: scripts/fetch-parakeet.sh
    to: scripts/fetch-parakeet.sh
asarUnpack:
  - resources/**
  - node_modules/better-sqlite3/**
  - node_modules/node-pty/**
  # The native addon and its dynamic libraries cannot load from inside asar. Its loader resolves
  # the platform package as a sibling, so both packages must be unpacked together.
  - node_modules/sherpa-onnx-node/**
  - node_modules/sherpa-onnx-darwin-arm64/**
win:
  executableName: vibechemy
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  extendInfo:
    - NSMicrophoneUsageDescription: Vibechemy uses the microphone for optional on-device push-to-talk dictation.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
